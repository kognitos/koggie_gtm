---
title: Browser Use | Books | Kognitos Documentation
url: https://docs.kognitos.com/books/reference-docs/browser-use
description: Overview of the Browser Use book.
---

# Browser Use | Books | Kognitos Documentation

The following documentation is forBrowser Use v1.14.0(BDK).

## hashtagOverview

TheBrowser Usebook enables you to performweb automationusing natural language. Use plain English to describe a task, and a remote browser agent will execute it.

The browser agent can perform a range of web automation tasks, including navigating pages, filling out forms, and extracting structured data, all with full traceability. Every action is logged with detailed step plans and unique session IDs. Take a look at this quick example:

## hashtagPrerequisites

### hashtag1. Required Books

The following Book(s) need to be added to your agent so it can learn and understand the automation procedures defined within them:

- Browser Use

Browser Use

#### hashtagHow to Add the Book(s)

- Go toBooks→All Books.

Go toBooks→All Books.

- Search for the name of the book and click on it.

Search for the name of the book and click on it.

- Click onInstallorAdd Connectionto add the book to your agent.

Click onInstallorAdd Connectionto add the book to your agent.

- If adding a connection, you'll be prompted forconnectivitydetails.

If adding a connection, you'll be prompted forconnectivitydetails.

Create anewplayground after adding the book so the agent can recognize its procedures.

## hashtagConnectivity

This section outlines the available methods for connecting to the Book, along with the required configuration details for each.

TheBrowser Usebook can be used with or without credentials.

## hashtagGetting Started

Follow these steps to write an automation that performs a web-based task:

#### hashtagProvision a Browser

The first step is toprovision a browser, which sets up abrowser instancefor automation to control. This is required for any browser automation task. In the editor, write:

You can alsonamethe browser instance using "as". This allows you to run multiple browser sessions or refer to a specific one. In this example, the browser instance is namedx:

#### hashtagPrepare to Perform a Task

On the following line, write thisprocedureto send the task to a remote browser agent:

If you assigned anameto the browser instance, use it in place ofthe browser:

#### hashtagCreate a Web Automation Task

Replace<the task>in the previous line with aBrowser Automationwidget:

- In the editor, type/to open the widget menu and selectBrowser Automation.

In the editor, type/to open the widget menu and selectBrowser Automation.

Don't see the widget in your menu? Contact our support team to have it enabled in your agent.

- Within the widget, selectChoose an Existing Taskor configure a new task by providing the following details. Then, clickSaveto finalize it.

Within the widget, selectChoose an Existing Taskor configure a new task by providing the following details. Then, clickSaveto finalize it.

A short, descriptive name for the task (e.g.,Check Website for Updates)

Task Instructions

Describe what you want the browser to do in plain English (e.g.,Go to example.com, log in, and check for new messages).

Output Format(Optional)

Define how you want the results formatted. It is recommended to use key-value pairs for clarity (e.g., {"key": "value"}). If specified, the agent will format the result accordingly.

Credentials(Optional)

If your task requires access to a secure website or account, provide the necessary credentials(e.g., username and password). You can select from existing connections in your agent or add a new connection.

#### hashtagRun Your Automation

After provisioning the browser and defining your task, you’re ready torunthe automation.
The browser agent will carry out the steps and return the following:

A screen-by-screen view of what happened in the browser.

Detailed Plan

The step-by-step breakdown of actions taken.

The output of the task(e.g., extracted data).

Browser Run ID

A unique identifier for the automation run.

Browser Files

List of files generated by the agent upon completing the browser automation task.

Learn how towork with data and filesin browser automation tasks.

## hashtagBest Practices

Follow these guidelines when configuring theBrowser Automationwidget to write clear and accurate task instructions for the browser agent.

#### hashtag1. Use Sequential Task Instructions

Write task instructions as a step-by-step script, where each line represents a distinct action in the workflow. For example:

#### hashtag2. Define the Output Format

Specify theOutput Formatto ensure the data is returned in an expected format. For example:

#### hashtag3. Be Precise

Refer to webpage elements using theirexact visible text or labelto prevent ambiguity.

- Click the 'Download Report' button.

Click the 'Download Report' button.

- Type the data into the field labeled 'Customer ID'.

Type the data into the field labeled 'Customer ID'.

❌Instead of

- Click the button.

Click the button.

- Find the table.

Find the table.

#### hashtag4. Use Dynamic Input

Use facts to dynamicallypass datainto your task, rather than hardcoding values.

Search for the client profile that has the name {the client name}.

❌Instead of

Search for "John Doe".

Last updated4 days ago

Was this helpful?
